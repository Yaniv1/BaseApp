{
    "REQUIREMENTS": [
        {
            "id": "BASE-REQ-001",
            "title": "Load and Resolve Layered Runtime Configuration",
            "description": "The system must load base and overlay configuration files, evaluate runtime expressions, and populate placeholders to produce one resolved runtime configuration object.",
            "priority": "Critical",
            "comments": [],
            "breakdown": [
                {
                    "id": "BASE-REQ-001.1",
                    "description": "Input requirement: The system shall read base configuration and merge JSON overrides from adjacent configuration files.",
                    "priority": "Critical",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-001.2",
                    "description": "Process requirement: The system shall evaluate expressions in COMMON and resolve placeholder references recursively using configurable wrapper tokens.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-001.3",
                    "description": "Output requirement: The system shall expose resolved configuration attributes through a dictionary-like object with dot-access.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-001.4",
                    "description": "Verification requirement: Runtime execution and prep tests shall confirm required configuration values are present and populated.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-001.5",
                    "description": "Local override requirement: The system shall load config/local.json when present and deep-merge its values on top of base and app config so that local overrides take effect without modifying tracked config files.",
                    "priority": "High",
                    "comments": []
                }
            ],
            "solution": [
                {
                    "feature": "6.1.3",
                    "role": "Params container merges nested config values and supports evaluate/populate flow.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "6.1.4",
                    "role": "Config orchestrates load, merge, evaluate, and placeholder population including optional local.json overrides.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "6.1.0",
                    "role": "config/local.json provides a gitignored placeholder for local-only config overrides deployed once via once.json manifest.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                }
            ]
        },
        {
            "id": "BASE-REQ-002",
            "title": "Provide Structured Logging and Runtime Monitoring",
            "description": "The system must log structured runtime events with message codes, typed severity, caller lineage, and optional data payloads while maintaining a monitorable in-memory state map.",
            "priority": "Critical",
            "comments": [],
            "breakdown": [
                {
                    "id": "BASE-REQ-002.1",
                    "description": "Input requirement: The system shall load message dictionaries from configured CSV sources.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-002.2",
                    "description": "Process requirement: The logger shall resolve message type and text by code, persist events, and include caller stack lineage metadata.",
                    "priority": "Critical",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-002.3",
                    "description": "Output requirement: The system shall write logs to CSV, optionally render logs to HTML, and enforce retention limits.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-002.4",
                    "description": "Structure requirement: The logger shall maintain a data_map snapshot and support dotted-path value resolution for test monitoring and diagnostics.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-002.5",
                    "description": "Output requirement: The runtime shall group logs by TYPE (INFO/GOOD/WARN/ERROR), persist grouped artifacts via existing output storage, and in summary mode print only a concise ERROR summary to the console.",
                    "priority": "Medium",
                    "comments": []
                }
            ],
            "solution": [
                {
                    "feature": "6.1.7",
                    "role": "Loads message lookup tables from configured CSV files and folders.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "6.1.8",
                    "role": "Factory resolves logger settings and constructs logger instances.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "6.1.9",
                    "role": "Logger stores typed events, caller lineage, data map snapshots, and CSV/HTML artifacts.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "6.1.9.24",
                    "role": "Logger.print_summary emits console summary output.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "5.3.1.3.8",
                    "role": "Post test validates per-type logs_by_type output artifacts after runtime completion.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                }
            ]
        },
        {
            "id": "BASE-REQ-003",
            "title": "Load Input Data from Files and Folders",
            "description": "The system must load configured input sources from file or folder paths into runtime results, including optional delta loading based on file modification times.",
            "priority": "Critical",
            "comments": [],
            "breakdown": [
                {
                    "id": "BASE-REQ-003.1",
                    "description": "Input requirement: The system shall support file and directory input paths with configurable format detection and relative-path resolution.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-003.2",
                    "description": "Process requirement: The loader shall support JSON, CSV, and text payloads and load items concurrently.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-003.3",
                    "description": "Performance requirement: In delta mode, unchanged files shall be skipped and only new or modified files shall be loaded.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-003.4",
                    "description": "Verification requirement: Prep tests shall validate delta behavior for first load, unchanged cycle, modified file, and added file scenarios.",
                    "priority": "High",
                    "comments": []
                }
            ],
            "solution": [
                {
                    "feature": "6.2.2",
                    "role": "DataLoader implements input source scanning, file loading, and delta mtime logic.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "6.1.11.1",
                    "role": "AppManager coordinates configured input loading and persists per-input delta metadata.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "5.3.1.2.1",
                    "role": "Input delta prep test verifies expected loader behavior across cycles.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                }
            ]
        },
        {
            "id": "BASE-REQ-004",
            "title": "Run Config-Driven Data Processing",
            "description": "The system must execute configured processing steps that transform loaded data using safe expression evaluation over DataFrame sources.",
            "priority": "High",
            "comments": [],
            "breakdown": [
                {
                    "id": "BASE-REQ-004.1",
                    "description": "Input requirement: Each process step shall define source data, optional context aliases, and ordered conversion rules.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-004.2",
                    "description": "Security requirement: Expression evaluation shall be constrained to an explicit safe globals set.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-004.3",
                    "description": "Output requirement: Each completed process step shall persist transformed data under a configured target.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-004.4",
                    "description": "Robustness requirement: Each conversion step in DataConverter.apply shall be individually wrapped in try-except. All exceptions (including missing source column) shall be logged via DATAE01 and appended to self.errors. Remaining steps shall continue executing after a failure. The errors list shall be reset at the start of each apply() call.",
                    "priority": "High",
                    "comments": []
                }
            ],
            "solution": [
                {
                    "feature": "6.2.1",
                    "role": "DataConverter applies secure, scope-aware conversions with per-step error and warning tracking.",
                    "implementation": 1.0,
                    "verification": 0.8,
                    "deployment": 1.0
                },
                {
                    "feature": "6.1.11.2",
                    "role": "AppManager executes process pipeline and stores target outputs.",
                    "implementation": 1.0,
                    "verification": 0.8,
                    "deployment": 1.0
                }
            ]
        },
        {
            "id": "BASE-REQ-005",
            "title": "Persist Runtime Outputs with Configurable Formats and Delta Behavior",
            "description": "The system must persist configured runtime artifacts in standard output formats, including nested split outputs and template-based HTML rendering.",
            "priority": "Critical",
            "comments": [],
            "breakdown": [
                {
                    "id": "BASE-REQ-005.1",
                    "description": "Output requirement: The system shall serialize runtime data to JSON and CSV with robust conversion of non-primitive values.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-005.2",
                    "description": "Output requirement: The system shall render HTML artifacts using configurable templates and wrappers.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-005.3",
                    "description": "Structure requirement: The system shall support split output generation across one or more nested dictionary levels.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-005.4",
                    "description": "Performance requirement: When output delta mode is enabled, unchanged artifacts shall be skipped using checksum and persisted manifest validation.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-005.5",
                    "description": "Verification requirement: Prep tests shall validate output delta behavior including skip, rewrite, and manifest persistence paths.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-005.6",
                    "title": "Concurrent Output Storage",
                    "description": "The system must support concurrent execution of output storage operations so that independent output artifacts are written in parallel, reducing total output phase elapsed time when multiple outputs are configured.",
                    "priority": "High",
                    "comments": [],
                    "breakdown": [
                        {
                            "id": "BASE-REQ-005.6.1",
                            "description": "Performance requirement: When OUTPUT_WORKERS > 1 is configured, each independent entry in CONFIG.OUTPUT shall be dispatched to a thread pool and written concurrently.",
                            "priority": "High",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-005.6.2",
                            "description": "Configuration requirement: The worker count shall be stored in CONFIG.COMMON.OUTPUT_WORKERS. A value of 0 or 1 shall retain the existing sequential behavior.",
                            "priority": "High",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-005.6.3",
                            "description": "Safety requirement: The shared output_manifest dict and its disk flush must be protected from concurrent access by a threading lock.",
                            "priority": "High",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-005.6.4",
                            "description": "Safety requirement: A per-worker exception in concurrent mode must be caught, logged via a dedicated WARN message code, and must not abort other in-flight workers.",
                            "priority": "High",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-005.6.5",
                            "description": "Verification requirement: A prep test shall confirm that sequential mode produces correct artifacts, concurrent mode produces the same artifacts, and a worker exception is logged as WARN while remaining outputs are still written.",
                            "priority": "High",
                            "comments": []
                        }
                    ]
                },
                {
                    "id": "BASE-REQ-005.7",
                    "description": "Code quality requirement: The JSON serialization helper to_json_compatible shall be promoted to a module-level function in baseutils so that it can be reused across the codebase independently of save().",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-005.8",
                    "title": "Output File Mapping in RESULTS",
                    "description": "The system must record a mapping of each output artifact label to the list of file paths that were actually stored (or confirmed present via delta-skip) in OUTPUT_MAP after each store_outputs call, so that downstream steps can inspect, report, or forward the stored file list.",
                    "priority": "Medium",
                    "comments": [],
                    "breakdown": [
                        {
                            "id": "BASE-REQ-005.8.1",
                            "description": "Tracking requirement: _save_output_artifact shall return the resolved file path whenever the artifact is written or confirmed present in delta-skip mode, and None when data is absent or the save fails.",
                            "priority": "Medium",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-005.8.2",
                            "description": "Tracking requirement: _store_split_outputs shall collect and return a flat list of all paths returned by recursive _save_output_artifact calls.",
                            "priority": "Medium",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-005.8.3",
                            "description": "Tracking requirement: _store_one_output shall return the list of file paths produced for its output key.",
                            "priority": "Medium",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-005.8.4",
                            "description": "Output requirement: store_outputs shall initialize OUTPUT_MAP as an empty dict in AppManager.__init__ and deep-update it with {output_key: [paths]} for every processed output entry, in both sequential and concurrent modes, without resetting between calls.",
                            "priority": "Medium",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-005.8.5",
                            "description": "Output requirement: OUTPUT_MAP shall itself be persisted as a JSON output artifact (output_map.json) under the configured OUTPUT_PATH/results directory.",
                            "priority": "Low",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-005.8.6",
                            "description": "Verification requirement: A prep test shall confirm that OUTPUT_MAP contains all expected keys, all paths exist on disk, accumulates across calls, and concurrent mode produces the same mapping.",
                            "priority": "Medium",
                            "comments": []
                        }
                    ]
                }
            ],
            "solution": [
                {
                    "feature": "6.1.6",
                    "role": "HtmlDoc renders structured tabular HTML outputs from runtime data.",
                    "implementation": 1.0,
                    "verification": 0.9,
                    "deployment": 1.0
                },
                {
                    "feature": "6.1.10",
                    "role": "save() persists artifacts in json/csv/html formats.",
                    "implementation": 1.0,
                    "verification": 0.9,
                    "deployment": 1.0
                },
                {
                    "feature": "6.1.17",
                    "role": "to_json_compatible() provides module-level recursive JSON serialization, called by save() and available for reuse across the codebase.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "6.1.11.3",
                    "role": "AppManager drives output storage, split logic, and checksum-manifest delta behavior.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "5.3.1.3.1",
                    "role": "Output delta prep test validates delta skip and overwrite scenarios.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "6.1.11.3",
                    "role": "store_outputs reads OUTPUT_WORKERS; when > 1 dispatches each enabled output entry to a ThreadPoolExecutor and collects exceptions.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 0.0
                },
                {
                    "feature": "6.1.11.9",
                    "role": "_store_one_output encapsulates resolve/convert/save for one output key, callable from both sequential loop and thread pool.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 0.0
                },
                {
                    "feature": "6.1.11",
                    "role": "AppManager.__init__ initializes _output_manifest_lock to serialize manifest reads/writes across worker threads.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 0.0
                },
                {
                    "feature": "6.1.11.7",
                    "role": "_save_output_artifact wraps output_manifest read and write+flush in _output_manifest_lock.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 0.0
                },
                {
                    "feature": "6.1.11.10",
                    "role": "store_outputs deep-updates OUTPUT_MAP with {output_key: [paths]} for every processed output entry in both sequential and concurrent modes; OUTPUT_MAP is initialized in __init__ and never reset.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 0.0
                },
                {
                    "feature": "5.3.1.3.4",
                    "role": "test_output_file_mapping prep test validates OUTPUT_MAP init, keys, path existence, accumulation across calls, concurrent mode parity, return value, and OUTPUT_MAP-sourced output written with full map.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 0.0
                }
            ]
        },
        {
            "id": "BASE-REQ-006",
            "title": "Execute Standardized Runtime Lifecycle",
            "description": "The system must execute the runtime lifecycle in deterministic order, capture runtime metrics, and return an explicit process exit code.",
            "priority": "Critical",
            "comments": [],
            "breakdown": [
                {
                    "id": "BASE-REQ-006.1",
                    "description": "Process requirement: Runtime shall execute load, process, close, and store-output stages in order.",
                    "priority": "Critical",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-006.2",
                    "description": "Output requirement: Runtime shall capture elapsed duration and include execution tracking metrics.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-006.3",
                    "description": "Safety requirement: Runtime shall return non-zero exit code on unhandled runtime errors or failed required tests.",
                    "priority": "High",
                    "comments": []
                }
            ],
            "solution": [
                {
                    "feature": "6.1.1",
                    "role": "App entrypoint class hosts concrete runtime instance.",
                    "implementation": 1.0,
                    "verification": 0.9,
                    "deployment": 1.0
                },
                {
                    "feature": "6.1.11.5",
                    "role": "AppManager.run enforces lifecycle stage order.",
                    "implementation": 1.0,
                    "verification": 0.9,
                    "deployment": 1.0
                },
                {
                    "feature": "6.1.1",
                    "role": "run() in app/base.py initializes config/logger, orchestrates tests, and emits final exit code.",
                    "implementation": 1.0,
                    "verification": 0.9,
                    "deployment": 1.0
                }
            ]
        },
        {
            "id": "BASE-REQ-007",
            "title": "Provide Built-In Multi-Phase Test Framework",
            "description": "The system must provide a configuration-driven test harness with prep, live, and post phases, structured result reporting, and independent test logging.",
            "priority": "Critical",
            "comments": [],
            "breakdown": [
                {
                    "id": "BASE-REQ-007.1",
                    "description": "Input requirement: Tests shall be declared by phase with callable targets, inputs, schedules, and pass/fail policies.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-007.2",
                    "description": "Process requirement: The harness shall execute prep tests synchronously, live tests on a scheduler thread, and post tests during shutdown.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-007.3",
                    "description": "Output requirement: The harness shall emit structured per-test result lines, phase summaries, aggregate reports, and dedicated test logs.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-007.4",
                    "description": "Safety requirement: Fail-fast behavior and fail-on statuses shall be configurable.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-007.5",
                    "description": "Deployment requirement: The framework shall support a standalone build phase test runner (test/tests/build.py) that executes build phase tests during deployment ceremonies, including running external deployment validation scripts and reporting per-criterion pass/fail results.",
                    "priority": "High",
                    "comments": []
                }
            ],
            "solution": [
                {
                    "feature": "6.3.1",
                    "role": "TestManager executes configured test phases and builds summaries/reports.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "6.3.2",
                    "role": "build_test_config merges runtime and testing configuration trees.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "6.3.3",
                    "role": "evaluate_checks validates declarative criteria in tests.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "6.3.4",
                    "role": "monitor_activity verifies runtime monitor behavior during live phase.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "5.3.2",
                    "role": "test/tests/build.py is the standalone build phase runner that loads config, runs the build phase, and stores outputs.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "6.3.5",
                    "role": "test_deployment runs a PowerShell deployment script and returns per-criterion structured results.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                }
            ]
        },
        {
            "id": "BASE-REQ-008",
            "title": "Synchronize Base and Variant Assets via Manifests",
            "description": "The system must support manifest-driven app instantiation and base-update pull workflows with deterministic copy behavior and run logging.",
            "priority": "High",
            "comments": [],
            "breakdown": [
                {
                    "id": "BASE-REQ-008.1",
                    "description": "Input requirement: Instantiation and pull workflows shall parse manifests from docs/manifests and validate schema.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-008.2",
                    "description": "Process requirement: Instantiation shall apply pull entries always and once entries only when destination files are absent.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-008.3",
                    "description": "Process requirement: Pull workflow shall support normal pull mode and hard overwrite mode.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-008.4",
                    "description": "Output requirement: Both workflows shall produce typed log artifacts and clear operation summaries.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-008.5",
                    "title": "Pullbase Syncs Newly Added Once Items to Existing Apps",
                    "description": "The pullbase script must apply once-manifest entries to an existing variant app by copying each source file or directory only when the destination does not already exist, so that newly introduced once items (e.g., requirements, tasks) are propagated without overwriting app-specific customizations.",
                    "priority": "High",
                    "comments": [],
                    "breakdown": [
                        {
                            "id": "BASE-REQ-008.5.1",
                            "description": "Process requirement: The system shall read 'once' entries from all synced manifest files during a normal (non-hard) pullbase run.",
                            "priority": "High",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-008.5.2",
                            "description": "Process requirement: For each once entry, the system shall copy the source file or directory contents to the local app only when the destination path does not already exist.",
                            "priority": "High",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-008.5.3",
                            "description": "Output requirement: The system shall report the count of newly copied once files and the count of skipped (already existing) once files.",
                            "priority": "Medium",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-008.5.4",
                            "description": "Verification requirement: Missing source files from once entries shall be reported in the same missing-sources warning flow as pull entries.",
                            "priority": "Medium",
                            "comments": []
                        }
                    ]
                },
                {
                    "id": "BASE-REQ-008.6",
                    "title": "Config Override Application During Instantiation",
                    "description": "The instantiate script must accept optional config overrides (via a JSON file path or an inline JSON string) and deep-merge them into config/app.json after APP_NAME is set, so that the instantiated app is pre-configured without requiring manual edits.",
                    "priority": "Medium",
                    "comments": [],
                    "breakdown": [
                        {
                            "id": "BASE-REQ-008.6.1",
                            "description": "Input requirement: The instantiate CLI shall accept an --overrides argument that accepts either a path to a JSON file or an inline JSON object string.",
                            "priority": "Medium",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-008.6.2",
                            "description": "Process requirement: The system shall deep-merge the override dict into config/app.json after the APP_NAME placeholder is set, preserving existing nested keys not present in the overrides.",
                            "priority": "Medium",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-008.6.3",
                            "description": "Output requirement: A structured log entry shall record which top-level override keys were applied to the config.",
                            "priority": "Low",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-008.6.4",
                            "description": "Verification requirement: A prep test shall confirm that APP_NAME is set correctly, that override values appear in config/app.json, and that keys not present in overrides retain their original values.",
                            "priority": "Medium",
                            "comments": []
                        }
                    ]
                }
            ],
            "solution": [
                {
                    "feature": "3.1.1",
                    "role": "Manifest loader reads and validates instantiate entries.",
                    "implementation": 1.0,
                    "verification": 0.8,
                    "deployment": 1.0
                },
                {
                    "feature": "3.1.2",
                    "role": "instantiate applies pull/once behavior and placeholder creation.",
                    "implementation": 1.0,
                    "verification": 0.8,
                    "deployment": 1.0
                },
                {
                    "feature": "3.2.1",
                    "role": "hard_pull overwrites all files in hard mode.",
                    "implementation": 1.0,
                    "verification": 0.8,
                    "deployment": 1.0
                },
                {
                    "feature": "3.2.2",
                    "role": "pull_base_files synchronizes pull-scoped files and folders.",
                    "implementation": 1.0,
                    "verification": 0.8,
                    "deployment": 1.0
                },
                {
                    "feature": "3.2.13",
                    "role": "pull_once_files copies once-manifest items to local app only when destination is absent, returning (copied, skipped, missing) counts.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 0.0
                },
                {
                    "feature": "3.2.12",
                    "role": "load_once_entries reads once entries from synced manifest files, parallel to load_pull_entries.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 0.0
                },
                {
                    "feature": "3.2.3",
                    "role": "main() loads once entries via load_once_entries, invokes pull_once_files, and extends missing_sources for unified warning reporting.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 0.0
                },
                {
                    "feature": "3.1.12",
                    "role": "load_overrides parses --overrides argument as a JSON file path or inline JSON string and returns the override dict.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "3.1.6",
                    "role": "populate_app_config now accepts optional overrides dict and deep-merges it into config/app.json after setting APP_NAME.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                }
            ]
        },
        {
            "id": "BASE-REQ-009",
            "title": "Maintain Architecture Compliance Inventory",
            "description": "The system must verify consistency between code and architecture inventories and generate review artifacts with actionable mismatch diagnostics.",
            "priority": "High",
            "comments": [],
            "breakdown": [
                {
                    "id": "BASE-REQ-009.1",
                    "description": "Input requirement: The test shall read architecture inventories and parse codebase items including files, classes, methods, and functions.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-009.2",
                    "description": "Process requirement: The comparator shall detect missing items, identifier mismatches, and path/name mismatches.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-009.3",
                    "description": "Output requirement: The test shall emit compliance reports and candidate architecture updates under test result folders.",
                    "priority": "Medium",
                    "comments": []
                }
            ],
            "solution": [
                {
                    "feature": "5.3.1.1.1",
                    "role": "ArchitectureAlignmentTest compares architecture inventory to parsed code and emits artifacts.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                }
            ]
        },
        {
            "id": "BASE-REQ-010",
            "title": "Expose Requirements and Tasks as Runtime Data and HTML Artifacts",
            "description": "The system must load requirements and task catalogs as standard runtime inputs and publish them as HTML output artifacts.",
            "priority": "High",
            "comments": [],
            "breakdown": [
                {
                    "id": "BASE-REQ-010.1",
                    "description": "Input requirement: The system shall load docs/requirements JSON files into runtime results.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-010.2",
                    "description": "Input requirement: The system shall load docs/tasks JSON files into runtime results.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-010.3",
                    "description": "Output requirement: The system shall render requirements runtime data as HTML artifacts.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-010.4",
                    "description": "Output requirement: The system shall render tasks runtime data as HTML artifacts.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-010.5",
                    "description": "Verification requirement: Post tests shall confirm requirements/tasks input load flags are present in monitor state.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-010.6",
                    "description": "Processing requirement: The system shall support grouping tasks by status field using config-driven data conversion and generate separate JSON and HTML output artifacts for each status group.",
                    "priority": "High",
                    "comments": [],
                    "breakdown": [
                        {
                            "id": "BASE-REQ-010.6.1",
                            "description": "Process requirement: The system shall execute config.PROCESS steps with custom conversions that transform raw input data structures into grouped outputs.",
                            "priority": "High",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-010.6.2",
                            "description": "Conversion requirement: The system shall support grouping tasks loaded from multiple source files by status field using data conversion expressions.",
                            "priority": "High",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-010.6.3",
                            "description": "Output requirement: The system shall render grouped task data to separate JSON files (one per status) and HTML artifacts using split output configuration.",
                            "priority": "High",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-010.6.4",
                            "description": "Verification requirement: Post tests shall confirm tasks_by_status process output contains all status groups with correct task filtering.",
                            "priority": "Medium",
                            "comments": []
                        }
                    ]
                }
            ],
            "solution": [
                {
                    "feature": "6.1.11.1",
                    "role": "AppManager.load_data loads requirements and task sources under configured INPUT targets.",
                    "implementation": 1.0,
                    "verification": 0.9,
                    "deployment": 1.0
                },
                {
                    "feature": "6.1.11.3",
                    "role": "AppManager.store_outputs renders requirements/tasks split HTML outputs through HtmlDoc.",
                    "implementation": 1.0,
                    "verification": 0.9,
                    "deployment": 1.0
                },
                {
                    "feature": "6.3.3",
                    "role": "Post-run evaluate_checks verifies requirements/tasks loaded flags.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "6.1.12.1",
                    "role": "AppManager.process_data executes PROCESS steps with custom scope conversions on loaded input data.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "6.1.12.2",
                    "role": "tasks_by_status process step transforms raw tasks dict into status-keyed groups via custom conversion expression.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "6.1.11.4",
                    "role": "AppManager.store_outputs renders tasks_by_status split JSON and HTML outputs to reports subfolder.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                }
            ]
        },
        {
            "id": "BASE-REQ-012",
            "title": "Automate Deployment Environment Setup and Ceremony",
            "description": "The system must provide a dedicated environment setup script that creates the Python virtual environment when absent and installs only missing dependencies, and a deployment ceremony script that calls the setup script then validates the deployment via integration and build tests.",
            "priority": "High",
            "comments": [],
            "breakdown": [
                {
                    "id": "BASE-REQ-012.1",
                    "description": "Environment requirement: setup_env.ps1 shall create a Python virtual environment (.venv) in the app root when one does not already exist.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-012.2",
                    "description": "Dependency requirement: setup_env.ps1 shall install all packages declared in dependencies/base.txt into the virtual environment.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-012.3",
                    "description": "Dependency requirement: setup_env.ps1 shall install all packages declared in dependencies/app.txt into the virtual environment.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-012.4",
                    "description": "Performance requirement: setup_env.ps1 shall compare declared packages against currently installed packages and invoke pip only for the missing subset, avoiding redundant installs on repeated runs.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-012.5",
                    "description": "Integration requirement: scripts/instantiate.py and scripts/pullbase.py shall call setup_env.ps1 after completing their file operations so that the environment is ready without requiring a separate manual step.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-012.6",
                    "description": "Validation requirement: deploy.ps1 shall call setup_env.ps1 as its first phase, then execute scripts/test_deployment.ps1 and test/tests/build.py as deployment validation phases.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-012.7",
                    "description": "Safety requirement: deploy.ps1 shall abort with a non-zero exit code if environment setup fails before attempting validation phases.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-012.8",
                    "title": "Deployment Validation Script",
                    "description": "The system must provide a standalone PowerShell script that exercises the full BaseApp deployment pipeline â€” instantiation of a fresh variant app followed by a base-update pull â€” and reports per-criterion PASS/FAIL results for pre, instantiate, pullbase, and post phases.",
                    "priority": "High",
                    "comments": [],
                    "breakdown": [
                        {
                            "id": "BASE-REQ-012.8.1",
                            "description": "Pre-test requirement: The script shall verify that all required BaseApp source files exist before attempting instantiation.",
                            "priority": "High",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-012.8.2",
                            "description": "Process requirement: The script shall invoke scripts/instantiate.py with a unique temp target path and assert exit code 0.",
                            "priority": "High",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-012.8.3",
                            "description": "Process requirement: The script shall verify that all expected pull-manifest and once-manifest files are present in the new TestApp after instantiation.",
                            "priority": "High",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-012.8.4",
                            "description": "Process requirement: The script shall invoke scripts/pullbase.py from the newly instantiated TestApp pointing back to BaseApp as source, and assert exit code 0.",
                            "priority": "High",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-012.8.5",
                            "description": "Post-test requirement: The script shall verify that pull-manifest items are present and that once-manifest items (e.g. config/app.json APP_NAME) were not overwritten by the pullbase run.",
                            "priority": "High",
                            "comments": []
                        },
                        {
                            "id": "BASE-REQ-012.8.6",
                            "description": "Configuration requirement: The script shall accept optional parameters for BaseApp root path, Python executable, and KeepTemp flag, and shall clean up temporary folders after the test.",
                            "priority": "Medium",
                            "comments": []
                        }
                    ]
                }
            ],
            "solution": [
                {
                    "feature": "3.4",
                    "role": "setup_env.ps1 creates the venv and installs missing base and app dependencies.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "3.5",
                    "role": "deploy.ps1 orchestrates the deployment ceremony: calls setup_env.ps1, then test_deployment.ps1, then build tests.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "3.1.11",
                    "role": "instantiate.py::run_setup_env calls setup_env.ps1 after instantiation completes.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "3.2.14",
                    "role": "pullbase.py::run_setup_env calls setup_env.ps1 after a successful base pull.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "3.3.1",
                    "role": "test_deployment.ps1 implements pre/instantiate/pullbase/post phases with per-criterion PASS/FAIL reporting, called by deploy.ps1 as the DEPLOYMENT TEST phase.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                }
            ]
        },
        {
            "id": "BASE-REQ-013",
            "title": "Consolidate Build, Documentation, and Data Resources into Dedicated Directories",
            "description": "The workspace file tree shall be reorganised so that build-related assets reside under build/ and shared runtime data resources reside under resources/, keeping docs/ exclusively for human-readable readme and version files.",
            "priority": "Medium",
            "comments": [],
            "breakdown": [
                {
                    "id": "BASE-REQ-013.1",
                    "description": "Structure requirement: A build/ directory shall contain instructions/, tasks/, requirements/, architecture/, and updates/ sub-folders, consolidating all build-related assets previously scattered under docs/ and the root updates/ folder.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-013.2",
                    "description": "Structure requirement: A resources/ directory shall contain message_codes/, templates/, and manifests/ sub-folders, consolidating all shared runtime data assets previously held under docs/.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-013.3",
                    "description": "Structure requirement: The docs/ directory shall retain only readme/ and version/ sub-folders after the consolidation.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-013.4",
                    "description": "Manifest requirement: pull.json entries shall use a target field to specify the destination path when it differs from the source path.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-013.5",
                    "description": "Manifest requirement: once.json entries shall use a destination field to specify the destination path when it differs from the source path.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-013.6",
                    "description": "Compatibility requirement: pullbase.py shall read the destination field (with fallback to target, then source) for once-manifest entries so that existing variant app files are not overwritten.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-013.7",
                    "description": "Compatibility requirement: instantiate.py shall read the destination field (with fallback to target, then source) for once-manifest entries.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-013.8",
                    "description": "Documentation requirement: The top-level README.md shall be maintained as a current-state overview of the app's capabilities and features, and shall not be a changelog. It shall not contain Highlights/Changelog/Release Notes/What's New sections, shall not enumerate releases, and shall not carry date- or version-stamped headings; per-release history shall live only in docs/readme/{base,app}.md and build/updates/{base,app}.json. An advisory build test shall report a non-blocking WARN when README.md drifts back into a changelog.",
                    "priority": "Medium",
                    "comments": []
                }
            ],
            "solution": [
                {
                    "feature": "10",
                    "role": "build/ directory consolidates instructions, tasks, requirements, architecture, and updates.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "11",
                    "role": "resources/ directory consolidates message_codes, templates, and manifests.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "11.3",
                    "role": "resources/manifests/ holds once.json (destination field) and pull.json (target field).",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "3.2.12",
                    "role": "load_once_entries reads destination field (fallback to target) for once-manifest entries.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "3.1.1",
                    "role": "load_manifest_entries reads destination field (fallback to target) for once-behavior entries.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                }
            ]
        },
        {
            "id": "BASE-REQ-014",
            "title": "Provide a Local Web-Based Task Management Interface",
            "description": "The system must provide a locally-hosted web interface that allows developers to create, update, add comments to, and view tasks stored in build/tasks/base.json without requiring an external service or hosting infrastructure.",
            "priority": "Low",
            "comments": [],
            "breakdown": [
                {
                    "id": "BASE-REQ-014.1",
                    "description": "Interface requirement: The web UI shall display all tasks grouped by status with a stat summary bar.",
                    "priority": "Low",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.2",
                    "description": "Create requirement: The UI shall allow creating a new task with title, description, type, priority, status, author, and an optional initial comment. On creation, the system shall automatically prepend a 'Task created: <title>' comment attributed to the selected author. The create form shall provide an author dropdown pre-populated with all previously seen comment authors (sorted alphabetically), defaulting to the last author alphabetically, while also allowing free-form entry of a new author name.",
                    "priority": "Low",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.3",
                    "description": "Update requirement: The UI shall allow editing task fields (title, type, priority, status, description) and appending new comments in-place.",
                    "priority": "Low",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.4",
                    "description": "Persistence requirement: All create/update/comment operations shall be persisted immediately to build/tasks/base.json.",
                    "priority": "Low",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.5",
                    "description": "Verification requirement: A prep test shall validate the load/create/update/comment round-trips and the HTTP API endpoints against a temp copy of the task store.",
                    "priority": "Low",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.6",
                    "description": "Lifecycle requirement: The system shall support a 'Ready' task state between 'InProgress' and 'Done'. The Copilot worker, when finished, shall set the task to 'Ready' (not 'Done') and stop without finalizing or pushing; only after explicit engineer approval shall the task move to 'Done', be finalized, committed, and pushed. The Task Manager UI shall display a dedicated 'Ready' tab. Activating Copilot on a 'Ready' task shall bring the existing, still-running worker window to the foreground when traceable; otherwise (window closed or the task completed elsewhere) it shall start a dedicated Copilot review session using a review-focused prompt that helps the engineer understand the changes and verify requirement fulfilment.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.7",
                    "description": "Deletion requirement: The UI shall allow deleting a task. Deleting a non-deleted task shall move it to a 'Deleted' status. The Task Manager UI shall display a dedicated 'Deleted' tab next to the other status tabs. Deleting a task that is already in the 'Deleted' status (from within the Deleted tab) shall permanently remove it from the task list. A deleted task cannot be acted upon (e.g. starting a Copilot worker) or modified (e.g. editing fields or adding comments) except for changing its status.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.8",
                    "description": "Startup sync requirement: When the Task Manager UI is activated (started), the system shall automatically synchronize only the currently selected app's task file with its own linked git repository before serving the UI, and shall block until that sync completes so the displayed task list reflects the synced state. The sync shall be skipped when no git repo is linked, and any failure shall be reported but shall not block UI startup. The automatic startup sync shall be skippable via the --no-startup-sync CLI flag.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.9",
                    "description": "Branch-and-deploy lifecycle requirement: Each task shall be worked on in its own short-lived, ad-hoc branch (e.g. 'task/<task-id>') created off main, never directly on the long-lived main branch, so that concurrent tasks do not mix their changes. When a worker is started from the Task Manager UI, the system shall automatically create and check out this task branch (via the dedicated launch_task_agent.ps1 launcher's -TaskBranch parameter) before launching the worker session. To allow multiple task agents to run in parallel -- a single working tree can hold only one checked-out branch at a time -- the repository shall use a bare shared object store with every branch (including main) checked out as its own peer git worktree under a single {APP} container directory (the bare store at {APP}/.bare, main at {APP}/main, each task at {APP}/<task-id>), and the system shall check the task branch out into its own dedicated worktree (a sibling of main, via the launcher's -Worktree parameter) and run the worker session inside it, rather than switching the main working tree in place. The worktree location is fixed and requires no configuration: every task worktree is always created as a sibling of main under the {APP} container (the parent of the main working tree), i.e. {APP}/<task-id>. The task's worktree shall be dissolved together with its branch (git worktree remove, then branch delete) once the task is integrated into main and reaches Done. Before starting a worker, the system shall first check whether a branch already exists for the task; if one does, it shall not silently start another worker but instead require the engineer to explicitly confirm (the start-agent endpoint returns HTTP 409 with requires_confirmation, and only proceeds when the request opts in via confirm_existing_branch), so that work is not unknowingly duplicated on a task already in progress. The lifecycle shall add a 'Deployed' status between 'Ready' and the terminal 'Done' status, giving the order ToDo -> InProgress -> Ready -> Deployed -> Done. 'Deployed' means the task is fully finalized and pushed to its own short-lived branch but not yet merged into main; 'Done' means the task is fully integrated into main, reached only after its branch is merged into main and dissolved with the integration engineer supervising the merge to ensure there are no unresolved conflicts (keeping 'Done' as the terminal status preserves backward compatibility). The Task Manager UI shall display a dedicated 'Deployed' status tab (with count badge and status styling) positioned between 'Ready' and 'Done', and shall offer 'Deployed' as a selectable status in the create and edit forms.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.10",
                    "description": "File-store task status channel requirement: Because each task may be worked on in its own branch while the Task Manager tracks only the main branch's task ledger, task workers (Copilot CLI agents) shall NOT update the task ledger by directly editing or committing build/tasks/<app>.json on their branch. Instead, an agent shall request every task-ledger change (status transitions and progress comments) by writing a request to a durable file store (the 'task status store'), and the Task Manager server shall be the sole writer that applies those requests to the authoritative ledger on main. The store shall be file-based and durable so that restarting or unexpectedly terminating the server never prevents an agent from recording progress: pending requests written while the server is down shall remain in the store and be applied once the server is available again. Each request shall be a single, atomically-written file (write-then-rename) whose content follows the task update template (build/tasks/update.template.json) -- a {\"TASKS\":[ ... ]} document where each item is identified by its task id and restricted to the updatable fields (status and/or a single comment item; worker_session is also accepted as a dict update). The server shall continuously sample (poll) the store at a configurable interval, validate each pending request, and apply it to a freshly loaded ledger by matching the task id and merging the request's fields by value type -- lists (comments) are combined (existing then incoming), dicts (worker_session) are updated recursively, and scalars (status) overwrite -- without using a generic dict deep-merge (rejected because TASKS is a list keyed by id and dict_merge has multiple unrelated dependents). The server shall then persist the ledger and stage, commit, and push it to the main branch headlessly, and move each processed request out of the pending area into a processed (or, on error, a failed) area for auditability; each request file shall be processed exactly once so a request is never applied twice. The location of the task status store shall be configurable (APP.TASK_MANAGER.status_queue / request_polling_frequency / enable; additional task-manager parameters are kept under APP.TASK_MANAGER, including prompt_store for worker prompt files and result_store for agent HTML work-summary/report files) and shall be provided to each agent through the task.md instruction template via a {status_store} placeholder (with the result store surfaced via a {result_store} placeholder), and build/instructions/base.md shall document the new request-via-store workflow (agents request status changes; the server samples the store and applies them to main) in place of agents committing the ledger themselves. To remove the burden of hand-writing request files, each worker agent shall additionally be given a per-agent stdio MCP server (scripts/status_queue_mcp.py, a thin 'enqueue' writer over the same durable store) -- launched and lifecycle-bound to that agent by launch_task_agent.ps1 via the Copilot CLI --additional-mcp-config hook -- exposing an enqueue_status_update(status?, comment?) tool that constructs the restricted request (the task id and comment author come from the agent's environment) and writes it atomically into the store's pending area. The dequeue side shall NOT be served by an MCP server: the Task Manager continues to consume the shared store directly from the filesystem, so the enqueue server is never on the critical path and the agent may fall back to writing the request file directly when the tool is unavailable. The instruction templates (task.md/base.md) shall present enqueue_status_update as the primary mechanism and                                        keep the direct file-write as a documented fallback.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.11",
                    "description": "Task Manager UI split-pane layout requirement: The Task Manager UI shall present the task list and the selected task's details as two fixed, vertically-stacked blocks (task list above, task details below) rather than rendering the details below the list within a single page-level scroll. Each block shall scroll independently within its own region so that scrolling the task list does not move the task details and vice versa, and the page as a whole shall not scroll. A draggable horizontal divider (gutter) between the two blocks shall let the user reallocate vertical space between them; the division shall default to 60% for the task list and 40% for the task details, and the chosen ratio shall be remembered across reloads. When no task is selected the details block shall show a placeholder prompting the user to select a task.",
                    "priority": "Low",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.12",
                    "description": "Status-sync ledger-isolation requirement: When the Task Manager server applies a queued status-update request and syncs the task ledger to git, that sync shall affect ONLY the task ledger (build/tasks/<app>.json) and shall never stage, commit, push, stash, rebase, or otherwise modify any other file in the worker's working tree. Because the shared working tree is, while a worker is active, checked out on the worker's short-lived task/<id> branch with uncommitted worker code/spec/test changes, the headless status-sync shall not commit the ledger onto the task branch and shall not run 'git add -A'/'commit -a' or a working-tree 'git pull --rebase --autostash' that could sweep the worker's uncommitted changes into the server's commits, push worker code ahead of the engineer's Ready/Deployed approval gate, or drop uncommitted worker changes via autostash. Instead, the server shall commit and push the ledger change in isolation from the worker's working tree -- via a dedicated detached git worktree built from the latest origin tip of the ledger branch -- so the ledger commit lands on the configured ledger branch (APP.TASK_MANAGER.ledger_branch, default main) with the ledger as its only changed path, and any rebase needed to integrate a moved remote occurs inside that isolated worktree (which only ever contains the ledger change) rather than in the worker's working tree. The ledger branch shall be configurable and the manual 'Sync Git' button / startup sync may continue to sync the currently checked-out branch, but always through the same working-tree-isolating mechanism. After the ledger commit is pushed to origin/<ledger-branch>, the server shall keep the primary 'main' worktree's local ledger branch from falling behind that remote tip: when (and only when) the primary working tree is checked out on the ledger branch and a pure fast-forward is possible (the local branch is an ancestor of origin/<ledger-branch>), it shall advance the local branch pointer to the pushed remote tip using a mixed reset that refreshes the branch pointer and index WITHOUT touching any working-tree files (so any legitimately modified ledger content or other working changes are preserved). It shall never reset over a diverged or ahead local branch, so un-pushed local commits are never clobbered.",
                    "priority": "High",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.13",
                    "description": "Task Manager UI task-list sorting and filtering requirement: The Task Manager UI task list shall let the user sort and filter the displayed tasks by each of its columns (ID, Title, Type, and Priority). Clicking a column header shall cycle that column's sort through three states -- none, ascending, then descending -- with a visible indicator of the active sort column and direction; the Priority column shall sort by severity rank (Critical > High > Medium > Low) rather than alphabetically. Each column shall also offer a filter restricting the rows to matching values: the ID and Title columns shall filter by case-insensitive substring text, and the Type and Priority columns shall filter by one or more values selected from a checkbox multi-select dropdown (matching any selected value). The Type and Priority filter multi-selects shall always offer the canonical values regardless of whether they currently appear in the task list (Type: Feature, Bug, Chore, Doc; Priority: Critical, High, Medium, Low), followed by any additional distinct values present in the data, with a way to clear that column's filter. Sorting and filtering operate within the currently selected status tab and are applied together (filters first, then sort). When active filters exclude every task in the current status the list shall indicate that no tasks match the current filters (distinct from the status simply being empty).",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.14",
                    "description": "Task Manager UI task-search requirement: The Task Manager UI shall provide a task-search bar positioned above the status tabs that lets the user locate a task by its task id. When the user submits a query (via the Search button or Enter), the system shall match it against task ids case-insensitively: an exact id match takes precedence, and when there is no exact match a unique case-insensitive substring match shall be accepted, while an ambiguous substring match (more than one task) shall prompt the user to enter a more specific id. When a task is found, the UI shall switch the active status tab to the matched task's (normalized) status, render that status's task list, select and scroll the matched row into view, and open the matched task's details in the details pane. When no task matches, the UI shall report an inline 'not found' message and leave the current view unchanged. The search shall reuse the existing status normalization, table rendering, and detail-display logic.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.15",
                    "description": "Task Manager UI all-tasks tab requirement: The Task Manager UI shall provide a single 'ALL' tab, positioned before (to the left of) the status tabs, that lists every task regardless of status (no division by status), and it shall be the default active tab shown on load. The ALL tab shall carry a count badge reflecting the total number of tasks. The same per-column sorting and filtering behavior available in the status tabs (ID, Title, Type, Priority) shall apply unchanged within the ALL tab. In addition, because the ALL tab mixes statuses, it shall expose a Status column that is available only in the ALL tab (and hidden in the per-status tabs, where it is redundant): the Status column header shall be sortable -- cycling none, ascending, then descending -- sorting by lifecycle order (the canonical status-tab order) rather than alphabetically, and shall offer a filter dropdown that restricts rows to a single exact (normalized) status, populated with the distinct statuses present in the task list plus an 'all' option that clears the filter. Status sorting and filtering shall combine with the other columns' filters and sort (filters first, then sort), and when active filters exclude every task the list shall indicate that no tasks match the current filters. Selecting a per-status tab shall continue to show only that status without the Status column.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.16",
                    "description": "Task Manager UI saved-views requirement: The Task Manager UI shall let the user store the current sort and per-column filter configuration as a named 'view' that can be quickly re-applied or deleted. Views are not first-class task entities -- each is purely a saved sort/filter configuration -- and are modelled as a single flat array of view objects (not a per-status dictionary). Each view object has the shape { id, name, tabs:[...], sort:{column:dir,...}, filter:{column:[values],...} }: 'tabs' is the list of tabs the view is associated with (a view may be associated with MULTIPLE tabs, and the 'ALL' tab is just another tab name); 'sort' is a compact ordered map listing only the sorted columns (its key order defines a MULTI-COLUMN sort precedence); and 'filter' is a compact map listing only the filtered columns, each mapping to an array of one or more values. A views bar shall appear directly below the status tabs bar (and below the task-search bar). On a given tab, EVERY view shall be presented as a chip, but a view is ENABLED (applicable) on that tab only when the current tab is in the view's 'tabs' list; views not associated with the current tab shall be shown grayed-out/disabled. A grayed-out user view shall offer a control to include the current tab (appending it to that view's 'tabs'), and an enabled user view shall offer a control to remove the current tab; a grayed-out built-in view's include control shall instead CLONE it into a new user view associated with the current tab (the built-in itself is never modified). A save icon/button shall save the live sort+filter configuration as a new user view associated with the current tab; the new view's name shall be auto-derived from its filters and sort (e.g. priority=High, title filter 'Task Manager', type=Bug yields 'High Priority Task Manager Bugs', with the type pluralized and an 'All <status> Tasks' fallback when no filters/sort are active). The user shall be able to rename a user-created view (e.g. by double-clicking its name) and to delete a user-created view via an X icon. The predefined built-in views shipped in config/base.json are fully read-only: they shall always be presented (clearly distinguished, with no delete control and no rename/tab-edit) and the user shall not be able to delete, rename, or change the tab associations of a built-in view; only the user's own views can be renamed, deleted, or re-tabbed. Clicking an enabled view chip shall make it the active view for that tab and apply its saved (multi-column) sort and (multi-value) filters to the list (reflected in the column sort indicators and the per-column filter controls). Manually changing a filter or sort shall deselect the active view unless the resulting configuration still exactly matches it. View definitions shall be stored locally and persist across sessions: a set of read-only built-in default views shall ship in the committed config/base.json under APP.TASK_MANAGER.views (a flat array), and the system shall serve, via a server /api/views GET/PUT endpoint (not browser localStorage), an effective payload { views:[...], active:{tab:viewId} } where 'views' is the built-in views (tagged builtin=true) followed by the user's saved views (tagged builtin=false) read from the override layers (config/local.json app-local, then config/machine.json machine-specific, machine taking precedence; a user view whose id collides with a built-in is dropped), and 'active' is the per-tab active-view map. Only the user's own views (never the built-ins) and the active-view map shall be persisted on disk to the machine-local, git-ignored config/machine.json under APP.TASK_MANAGER.views (a flat array) and APP.TASK_MANAGER.active_views, preserving every other key already present in that file, so built-in views always come from base.json (and cannot be deleted or re-tabbed) while a user's personal views and active-view selections stay machine-local and never pollute the committed configuration, surviving sessions and browsers. Each tab shall remember its last active view (via the active map) so that switching to that tab restores it (applying its sort/filters), while a tab with no active view restores an unfiltered, unsorted list. Saved views shall integrate with task search: locating a task by id clears the active view/filters for the matched status tab so the searched task is always visible.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.17",
                    "description": "Task Manager UI multi-column sort and multi-value filter requirement: Building on BASE-REQ-014.13, the Task Manager task list shall support sorting by MULTIPLE columns at once and filtering the Type and Priority columns by MULTIPLE values at once. The live sort state shall be an ordered list of (column, direction) entries; when more than one column is active the table shall sort by each entry in order (the first as primary, the next as tie-breakers) and the column sort indicators shall show the precedence position alongside the direction. A manual header click sets a single cycling sort column, but applying a saved view may impose a multi-column sort which is honored in order. The Type and Priority column filters shall each be a checkbox multi-select dropdown holding an array of selected values, matching a row when its value equals ANY selected value; an empty selection imposes no constraint on that column. These multi-column sort and multi-value filter configurations are exactly what saved views (BASE-REQ-014.16) capture in their compact 'sort' and 'filter' maps and round-trip through /api/views.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.18",
                    "title": "Branch-Aware Variant App Instantiation",
                    "description": "Variant-app multi-branch instantiation requirement: scripts/instantiate.py shall deploy a new variant app directly into the multi-branch bare/worktree directory layout instead of a flat single-tree folder. It shall accept --root (default '../../../' resolved relative to the script's directory, i.e. the parent of the BaseApp container, so running from c:/code/BaseApp/main/scripts targets c:/code/), --appName (default 'MyApp{YYYYMMDD}'), and --branch (default 'main'), and shall place the instantiated app at {abspath(root)}/{app-name}/{branch}. It shall additionally accept --worktree on|off (default 'on'): with 'on' it uses the multi-branch layout {abspath(root)}/{app-name}/{branch}, and with 'off' (legacy) it deploys flat at {abspath(root)}/{app-name} with no branch sub-folder (so the post-hoc migration path can be exercised). It shall ensure the deployed app carries a .gitignore that excludes the machine-specific config/local.json, .venv/, and __pycache__/, so a later post-hoc git initialization never commits those local-only or regenerable paths. APP_NAME shall be written to config/app.json COMMON.APP_NAME from --appName; the app's branch shall be implied by the worktree folder name and shall not be separately stored. The script shall persist a single absolute path to the BaseApp source branch folder it was instantiated from as COMMON.BASEAPP inside the app's gitignored, once-deployed config/local.json, maintained as the single source of truth in the app's main worktree, so that pull-back tooling can locate the base without re-discovery and without that local-only path being synced to git. instantiate shall NOT create any git metadata (.git, .bare, or git pointer files); it shall only lay down the directory structure so that a later, post-hoc git initialization into the bare/worktree layout can adopt the {APP}/{branch} directory non-destructively, without overwriting or deleting any instantiated file. Additionally, instantiate shall persist the chosen layout as COMMON.WORKTREE (a boolean) in the same gitignored config/local.json -- true for --worktree on, false for off -- so that post-hoc migration tooling can deterministically tell a flat deployment from a nested {APP}/{branch} one without having to infer it structurally. The operation shall remain machine-agnostic, with no hard-coded user, drive, or absolute path.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.19",
                    "title": "Branch-Aware Base-Pull Source Resolution",
                    "description": "Variant-app multi-branch base-pull requirement: scripts/pullbase.py shall locate its BaseApp source from the persisted COMMON.BASEAPP path rather than ad-hoc discovery, and shall accept --branch (default 'main') selecting which BaseApp worktree to pull updates from, resolving the source as {parent-of-BASEAPP}/{branch}. Because config/local.json is gitignored and therefore not materialized in freshly created task-branch worktrees, pullbase shall resolve COMMON.BASEAPP through a fallback chain: first the current worktree's config/local.json, then the app's main worktree config/local.json ({container}/main/config/local.json where {container} is the parent of the running worktree), then the pre-existing --baseSource/auto discovery for backward compatibility. local.json shall be maintained as the single source of truth in the app's main worktree; pullbase running inside a task-branch worktree shall read main's reference as a fallback without mutating main's local.json. To prevent data loss, pullbase shall refuse to run when the resolved source and the local app are the same deployment/repository (identical paths, one an ancestor of the other, sibling worktrees sharing a {container}/.bare, or two worktrees sharing the same git object store): such a self-pull would overwrite the running branch's in-progress changes with a sibling branch's files. A genuine variant app pulling from a separate BaseApp container shall be unaffected.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.20",
                    "title": "Migrate Existing Deployment to Multi-Branch Layout",
                    "description": "Existing-deployment multi-branch migration requirement: scripts/init_worktree.ps1 shall, in addition to cloning a remote into the bare/worktree layout, automatically migrate an EXISTING branch-agnostic single-tree app deployment (for example a flat instantiate output or a plain git checkout) in place into the multi-branch bare/worktree layout. Mode selection shall be automatic: when the resolved {APP} container already contains app content but no {APP}/.bare store, the script shall migrate that content in place; when the target is empty/new it shall clone from -Url as before. Migration shall promote the existing app directory to the {APP} container and relocate its contents into a {APP}/{branch} worktree (branch defaulting to the existing repo's current branch when it is a git repository, otherwise 'main'), producing the same canonical layout as the clone path: a bare object store at {APP}/.bare, a rename-safe relative .git pointer ('gitdir: ./.bare'), and {APP}/{branch} registered as a proper linked worktree (as reported by 'git worktree list --porcelain'). Migration shall be non-destructive: it shall preserve commit history when the source is already a git repository (and create an initial commit when it is not), and shall preserve untracked and gitignored working files that are not part of git history -- in particular the gitignored config/local.json carrying COMMON.BASEAPP -- excluding only regenerable artifacts (.venv, __pycache__). To guarantee this even when a legacy deployment carries no .gitignore of its own, migration shall add .venv/, __pycache__/, and config/local.json to the repo's local exclude (.git/info/exclude) before seeding history, so those paths are never committed and the machine-specific config/local.json is instead restored into the worktree as an ignored file. Migration shall also optionally accept -Url to attach a real 'origin' remote to the otherwise local-only migrated repo (configuring the standard fetch refspec without performing any fetch or push). It shall be idempotent (re-running against an already-migrated container, i.e. one that already has {APP}/.bare, safely skips), machine-agnostic, and shall not silently destroy or overwrite any existing app file. Layout detection shall be driven by the persisted COMMON.WORKTREE flag written by instantiate (config/local.json): a true flag (or, secondarily, an existing {APP}/.bare) indicates a nested {APP}/{branch} deployment whose per-branch content directory differs from the {APP} container, while an absent flag shall be treated as a legacy flat deployment (content directly in {APP}); when the flag is absent the script shall fall back to structural detection, and after a successful migration it shall set COMMON.WORKTREE=true. Accordingly the migration shall correctly handle BOTH a flat deployment (content in {APP}) and the DEFAULT nested deployment (content in {APP}/{branch} with no .bare, as produced by 'instantiate --worktree on'), relocating only the per-branch content directory and never double-nesting it. To avoid a 'directory in use' rename failure when the script is run from inside the deployment's own scripts folder, migration shall move both the process working location and the OS current directory out of the content tree before renaming it, and restore a valid location afterward. Migration shall further accept an optional -remote parameter that publishes the migrated bare/worktree repo to GitHub via the gh CLI: it shall ensure authentication, create the repository as PRIVATE when it does not already exist, set it as 'origin' with the standard fetch refspec, and push all local branches with upstream tracking; -remote shall accept an owner/name, a bare name, or a full https/ssh clone URL (a URL being normalized to owner/name for the gh calls), and when -remote is omitted no remote shall be created or contacted.",
                    "priority": "Medium",
                    "comments": []
                },
                {
                    "id": "BASE-REQ-014.21",
                    "title": "Multi-Branch Repository Initializer Script",
                    "description": "Multi-branch repository initialization requirement: As part of deploying variant apps into the same multi-branch dev architecture used by BaseApp, the framework shall provide a reusable, machine-agnostic PowerShell script (scripts/init_worktree.ps1) that clones a repository into the multi-branch bare/worktree layout rather than as a plain single-tree git clone. Given a clone URL (and optional root, container name, default branch, and task branch), the script shall create the {APP} container directory, perform a bare clone into {APP}/.bare, write a rename-safe relative .git pointer ('gitdir: ./.bare'), normalize the fetch refspec so every remote branch is tracked, fetch all branches, and check out the default branch as the {APP}/<default-branch> worktree, optionally adding a second worktree for a supplied task branch (created off the default branch when it does not yet exist). When -Root and/or -Name are omitted, the script shall derive them from its own deployed location (it lives at {APP}/{branch}/scripts): -Root shall default to the parent of that {APP} and -Name to that {APP}'s folder name (except that when -Url is supplied -Name shall default to the repository name inferred from the URL), so that a no-argument run from a deployed app's scripts folder targets that app's own container rather than the current working directory. The operation shall be idempotent so re-running against an existing container safely skips completed work, and shall not be hard-coded to any user, drive, or absolute path. The same script shall serve both BaseApp and variant apps (selected purely by the clone URL passed in), and because the whole scripts/ folder is propagated to variant apps via resources/manifests/pull.json, variant apps shall receive this initializer automatically, so that variant-app development benefits from the identical multi-branch architecture used by BaseApp.",
                    "priority": "Medium",
                    "comments": []
                }
            ],
            "solution": [
                {
                    "feature": "3.6",
                    "role": "scripts/task_manager.py provides the HTTP server, task store helpers, and single-page UI.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "3.6.1",
                    "role": "parse_args parses CLI arguments (--tasks-path, --host, --port, --open-browser).",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "3.6.2",
                    "role": "run() binds the server and serves until KeyboardInterrupt.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "3.6.5",
                    "role": "_activate_copilot_window traces a Ready task's recorded worker session (pid + window title) and re-focuses the still-running Copilot console window for review.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "3.6.6",
                    "role": "_build_review_prompt / _start_copilot_for_task(mode='review') start a dedicated review session using build/instructions/task-review.md when the worker window is not traceable.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "3.6.7",
                    "role": "_delete_task and the DELETE /api/tasks/<id> endpoint soft-delete a task to the 'Deleted' status or permanently remove it when already deleted; _update_task and the start-agent/comments endpoints restrict deleted tasks to status-only changes. The UI adds a 'Deleted' tab and a Delete button.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "3.6.8",
                    "role": "_sync_selected_app_on_startup syncs only the currently selected task store with its own linked git repo when run() starts the UI, reusing _sync_task_store_to_git; it is skipped when no repo is linked and failures are reported but never block startup. The --no-startup-sync flag disables it.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "5.3.1.7.1",
                    "role": "test_task_manager_interface prep test validates load/create/update/comment and HTTP round-trips. 6 criteria.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "3.6.9",
                    "role": "launch_task_agent.ps1 creates and checks out the task's own short-lived '<task-id>' branch (named after the task id with no 'task/' prefix, so it matches the worktree folder; via its -TaskBranch parameter) off the current HEAD before starting a work-mode worker session; _branch_name_for_task computes the branch name and _start_copilot_for_task passes it to the launcher (skipped for reviews) and records it on the task's worker_session. _task_branch_exists detects a pre-existing local or remote-tracking '<task-id>' branch, and the start-agent endpoint returns HTTP 409 (requires_confirmation) when one exists unless the request sets confirm_existing_branch, so the engineer must opt in before a second worker is started on an in-progress task; the UI prompts on this 409 and retries with confirm_existing_branch. The UI renders status tabs in the order ToDo, InProgress, Specified, Ready, Deployed, Approved, Done (each with count badge, badge styling, normalizeStatus synonyms, statusCls, and create/edit dropdown options for the lifecycle statuses), followed by the Deleted tab and a pathological 'Other' catch-all tab; unrecognized statuses normalize to 'Other' instead of collapsing into 'ToDo'. A single 'ALL' tab precedes the status tabs and is the default active tab: it lists every task without status division (currentStatus 'ALL' bypasses the per-status filter in getVisibleTasks), carries a total-count badge (cnt-ALL), and within it the task list exposes an extra Status column (sortable by lifecycle order and filterable by exact normalized status via filter-status) that is shown only in the ALL tab and hidden in the per-status tabs; the standard ID/Title/Type/Priority column sorting and filtering apply unchanged in the ALL tab.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 1.0
                },
                {
                    "feature": "3.6.10",
                    "role": "Durable file-store task status channel: agents request task-ledger changes (status transitions and comments) by writing atomic JSON request files -- following the build/tasks/update.template.json shape ({\"TASKS\":[{id,status,comments,worker_session}]}) -- into the configurable 'task status store' instead of editing/committing the ledger on their branch, and the Task Manager server is the sole writer that applies them to main. _resolve_status_store_paths/_status_inbox_dir resolve and create the pending/processed/failed layout from APP.TASK_MANAGER.status_queue/request_polling_frequency/enable; _process_status_inbox scans the pending area and, per file (_apply_status_request_file), loads a fresh ledger and applies each update via _apply_task_update (matched by task id; lists combine, dicts update recursively, scalars overwrite -- no generic dict_merge), persists the ledger, then stages/commits/pushes to main headlessly via _sync_task_store_to_git(headless=True), moving each file to processed/ or failed/. Each file is processed exactly once (idempotent, restart-safe); a git-sync failure is logged without re-failing the request. _status_inbox_watcher runs as a background daemon thread started by run() (skippable via --no-status-inbox) that continuously samples the live store at APP.TASK_MANAGER.request_polling_frequency. The pending path is surfaced to each agent through a {status_store} placeholder rendered into the task.md prompt by _build_copilot_prompt (which also renders {result_store} from APP.TASK_MANAGER.result_store); worker prompt files are written to APP.TASK_MANAGER.prompt_store. Agents normally enqueue requests through a per-agent stdio MCP server (scripts/status_queue_mcp.py, tool enqueue_status_update(status?, comment?)) that _start_copilot_for_task wires in via _write_agent_mcp_config and the launcher's --additional-mcp-config hook; the direct atomic file write remains the documented fallback. The dequeue side is not served by MCP -- the Task Manager reads the shared store directly.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 0.0
                },
                {
                    "feature": "3.7",
                    "role": "scripts/sync_task_repo.ps1 commits and pushes ONLY the task ledger, isolated from the worker's primary working tree via a dedicated detached git worktree built from the latest origin/<branch> tip (-Branch defaults to current HEAD; headless status-inbox passes APP.TASK_MANAGER.ledger_branch, default main). It path-scopes 'git add -- <ledger>', commits, and pushes to refs/heads/<branch>, rebasing only the single ledger commit inside the isolated worktree on non-fast-forward; it never runs 'git add -A'/'commit -a' or a working-tree autostash/rebase, so it cannot sweep, early-push, or drop the worker's uncommitted changes. task_manager.py: _resolve_ledger_branch resolves the ledger branch from config; _sync_task_store_to_git(branch=...) forwards -Branch; _apply_status_request_file passes the ledger branch for the headless sync.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 0.0
                },
                {
                    "feature": "5.3.1.7.2",
                    "role": "test_sync_task_repo_isolates_ledger_commit_from_worker_tree runs sync_task_repo.ps1 -Branch main against a clone on a task branch with uncommitted ledger and worker-file edits, asserting the ledger commit is isolated to origin/main, the worker's edit is preserved, HEAD stays on the task branch, and the ledger commit never lands on the task branch.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 0.0
                },
                {
                    "feature": "3.8",
                    "role": "scripts/init_worktree.ps1 clones any repo (BaseApp or variant app, selected by -baseRepo) into the bare/worktree multi-branch layout: bare clone into {APP}/.bare, rename-safe relative .git pointer, normalized fetch refspec, all-branch fetch, default-branch worktree, and an optional -taskBranch worktree. Idempotent and machine-agnostic; propagated to variant apps via resources/manifests/pull.json (scripts/).",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 0.0
                },
                {
                    "feature": "3.8.1",
                    "role": "Add-Worktree adds each branch's worktree subfolder idempotently: skips already-registered worktrees, leaves untracked existing folders untouched, checks out an existing local/remote branch, or creates a new branch off origin/<default-branch> when -CreateIfMissing is set.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 0.0
                },
                {
                    "feature": "5.3.1.8.1",
                    "role": "test_init_bare_worktree_repo_builds_bare_and_worktrees clones a local source repo via init_worktree.ps1 and asserts the .bare store, relative .git pointer, default-branch worktree with content, a new -TaskBranch worktree off the default branch, and idempotent re-run.",
                    "implementation": 1.0,
                    "verification": 1.0,
                    "deployment": 0.0
                },
                {
                    "feature": "3.1.14",
                    "role": "resolve_app_root computes the variant app root {abspath(root)}/{app-name}/{branch} from the --root (default script-relative '../../../'), --appName (default 'MyApp{YYYYMMDD}'), and --branch (default 'main') CLI args.",
                    "implementation": 0.0,
                    "verification": 0.0,
                    "deployment": 0.0
                },
                {
                    "feature": "3.1.15",
                    "role": "populate_local_config writes COMMON.BASEAPP (the absolute path of the BaseApp source branch folder instantiate ran from) into the app's gitignored, once-deployed config/local.json in the app's main worktree, without emitting any git metadata.",
                    "implementation": 0.0,
                    "verification": 0.0,
                    "deployment": 0.0
                },
                {
                    "feature": "3.2.19",
                    "role": "resolve_baseapp_source resolves the BaseApp source from COMMON.BASEAPP via a worktree->main-worktree->auto fallback chain and applies --branch by swapping the BASEAPP leaf ({parent-of-BASEAPP}/{branch}); backward compatible with --baseSource/auto.",
                    "implementation": 0.0,
                    "verification": 0.0,
                    "deployment": 0.0
                },
                {
                    "feature": "5.3.1.5.2",
                    "role": "test_instantiate_branch_aware_layout asserts instantiate places content under {container}/{app-name}/{branch} (default main and a custom --branch), sets app.json COMMON.APP_NAME from --appName, writes COMMON.BASEAPP into config/local.json, and emits no git metadata.",
                    "implementation": 0.0,
                    "verification": 0.0,
                    "deployment": 0.0
                },
                {
                    "feature": "5.3.1.5.3",
                    "role": "test_instantiate_layout_survives_git_init instantiates an app then performs a post-hoc in-place git init/adopt into the bare/worktree layout and asserts every instantiated file is preserved byte-for-byte (git-init safety).",
                    "implementation": 0.0,
                    "verification": 0.0,
                    "deployment": 0.0
                },
                {
                    "feature": "3.8.2",
                    "role": "Convert-ExistingDeployment migrates an existing branch-agnostic single-tree app (flat instantiate output or plain git checkout) in place into the canonical {APP}/.bare + {APP}/{branch} worktree layout: it commits/initialises history as needed, records untracked/gitignored files (preserving config/local.json, excluding .venv/__pycache__), bare-clones the history into {APP}/.bare, writes the .git pointer, registers {APP}/{branch} via 'git worktree add', and restores the preserved files. Non-destructive, idempotent, auto-selected when {APP} has content but no .bare.",
                    "implementation": 0.0,
                    "verification": 0.0,
                    "deployment": 0.0
                },
                {
                    "feature": "5.3.1.8.2",
                    "role": "test_init_bare_worktree_repo_migrates_existing_deployment builds a flat branch-agnostic app (incl. a gitignored config/local.json), runs the migration mode, and asserts the canonical bare/worktree layout (porcelain), preservation of all files including local.json, a clean working tree, attachable task worktrees, and idempotent re-run.",
                    "implementation": 0.0,
                    "verification": 0.0,
                    "deployment": 0.0
                },
                {
                    "feature": "5.3.1.4.2",
                    "role": "test_pullbase_baseapp_resolution_with_main_fallback asserts pullbase resolves COMMON.BASEAPP from the current worktree's local.json, falls back to the app's main worktree local.json when the task worktree has none, and applies --branch by swapping the BASEAPP leaf.",
                    "implementation": 0.0,
                    "verification": 0.0,
                    "deployment": 0.0
                }
            ]
        }
    ]
}
